import React from 'react'
import PropTypes from 'prop-types'
import { Link } from 'react-router-dom'

import styles from './footer.module.scss'

const Footer = props => {
  return <footer className={styles.wrap}>Footer</footer>
}

Footer.propTypes = {}

export default Footer
